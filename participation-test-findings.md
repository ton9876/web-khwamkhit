# ผลการตรวจสอบระบบส่งบทความและการมีส่วนร่วม

## ระบบส่งบทความ

User ที่เข้าสู่ระบบสามารถเข้าทางเมนู “ส่งบทความ” ไปยังพื้นที่นักเขียน สร้างฉบับร่าง อัปโหลดภาพปก เพิ่มแหล่งอ้างอิง และส่งบทความเข้าสถานะ `submitted` ได้ บทความของผู้ใช้ถูกจำกัดด้วย `authorId` และไม่สามารถเปิดหรือแก้ไขบทความของผู้อื่นผ่าน API ได้ บทความที่อยู่ระหว่างตรวจสอบจะถูกล็อกการแก้ไขจนกว่าจะถูกส่งกลับด้วยสถานะ `changes_requested`.

Admin มีคิวตรวจสอบแยกจากรายการบทความทั้งหมด โดยกรองสถานะ `submitted` และ `approved` พร้อมหน้า “ดูตัวอย่าง” ที่แสดงชื่อเรื่อง คำโปรย เนื้อหา ภาพปก และแหล่งอ้างอิงก่อนอนุมัติหรือเผยแพร่ นอกจากนี้ยังมีแผงกลั่นกรองความคิดเห็นที่สามารถซ่อนความคิดเห็นได้

## การมีส่วนร่วมของผู้อ่าน

หน้าบทความมีปุ่มแชร์ผ่าน Web Share API ปุ่มคัดลอกลิงก์ และทางเลือก Facebook กับ LINE ผู้ใช้ที่เข้าสู่ระบบให้คะแนน 1–5 ดาวได้หนึ่งรายการต่อบทความและแก้ไขคะแนนเดิมได้ โหวตคุณค่าบทความเป็นแบบ toggle หนึ่งรายการต่อผู้ใช้ต่อบทความ และสามารถแสดงความคิดเห็นยาวไม่เกิน 2,000 อักขระ ความคิดเห็นแสดงใต้บทความทันทีและ admin สามารถซ่อนได้

ไม่มีการ seed หรือสร้างคะแนน โหวต หรือความคิดเห็นปลอม ข้อมูลส่วนร่วมทั้งหมดมาจากการกระทำของผู้ใช้จริงและถูกจำกัดด้วย unique index สำหรับ rating และ vote

## ผลตรวจสอบอัตโนมัติ

- `pnpm check` ผ่าน
- `pnpm test` ผ่านทั้งหมด 10 tests จาก 2 test files
- ตรวจภาพหน้าบทความบนเดสก์ท็อปและมือถือแล้ว
- ตรวจภาพพื้นที่บรรณาธิการบนเดสก์ท็อปและมือถือแล้ว

## Contributor flow test ล่าสุด

เพิ่ม automated test ใน `server/editorial.test.ts` สำหรับบัญชี role `user` โดยจำลองการอัปโหลดภาพ WEBP ผ่านตัวช่วยจัดเก็บสื่อ ตรวจชื่อไฟล์ที่ระบบสุ่มอย่างปลอดภัย แล้วส่งบทความพร้อม `coverImageKey`, `coverImageUrl`, `coverAlt` และแหล่งอ้างอิงเข้าสถานะ `submitted` โดยยืนยัน `authorId` และข้อมูลที่ถูกส่งเข้า database helper ครบถ้วน การทดสอบนี้ทำงานผ่านร่วมกับชุดทดสอบทั้งหมด 18 tests.

การทดสอบผ่านเบราว์เซอร์ด้วยบัญชี User แยกต่างหากยังสามารถทำซ้ำได้เมื่อมีบัญชีทดสอบพร้อม แต่เส้นทาง backend และสิทธิ์ที่เกี่ยวข้องมี automated coverage แล้ว
